name: Publish medguardx-core to PyPI

# Publishes the core engine to PyPI when a GitHub Release is published, or on
# manual dispatch. Uses PyPI Trusted Publishing (OIDC) -- no API token/secret
# needed. Configure the trusted publisher once on PyPI (see DEPLOYMENT.md).

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # required for trusted publishing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Build sdist and wheel
        working-directory: packages/medguardx-core
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/medguardx-core/dist
